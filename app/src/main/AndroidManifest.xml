<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Visibilité des paquets : chaque app d'AppCatalog doit figurer ici, sinon elle
         paraît « non installée » (Android 11+). Shizuku : requis par sa bibliothèque. -->
    <queries>
        <package android:name="com.google.android.apps.youtube.music" />
        <package android:name="com.spotify.music" />
        <package android:name="deezer.android.app" />
        <package android:name="com.apple.android.music" />
        <package android:name="com.amazon.mp3" />
        <package android:name="com.soundcloud.android" />
        <package android:name="com.aspiro.tidal" />
        <package android:name="com.qobuz.music" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.twitter.android" />
        <package android:name="org.telegram.messenger" />
        <package android:name="tv.twitch.android.app" />
        <package android:name="org.videolan.vlc" />
        <package android:name="com.netflix.mediaclient" />
        <package android:name="au.com.shiftyjelly.pocketcasts" />
        <package android:name="com.android.chrome" />
        <package android:name="org.mozilla.firefox" />
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <!-- Notification crossfader : service de premier plan portant une MediaSession -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.DuoMix"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MixerNotificationService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Widget « Historique des détections » (écran d'accueil) -->
        <receiver
            android:name=".widget.HistoryWidgetProvider"
            android:exported="true"
            android:label="@string/widget_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/history_widget_info" />
        </receiver>

        <!-- Provider Shizuku : permet à l'app d'être découverte par Shizuku -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>
</manifest>
